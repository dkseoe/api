{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "Efficient reverse-proxy gateway with multi-concurrency support and upstream model-list aggregation.",
  "type": "module",
  "main": "src/server.js",
  "bin": {
    "api-gateway": "src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/**/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "undici": "^8.6.0"
  }
}
